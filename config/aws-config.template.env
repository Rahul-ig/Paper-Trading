# AWS Configuration Template
# Copy this file to aws-config.env and fill in your actual values
# 
# IMPORTANT: Most values below are AUTO-GENERATED during deployment!
# You only need to provide: AWS credentials, Account ID, and optional API keys.
# The deployment script will create all AWS resources and update ARNs automatically.

# AWS Account Configuration
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=YOUR_AWS_ACCOUNT_ID

# IAM User Credentials
# NEVER commit real credentials. Use `aws configure` or environment variables.
AWS_ACCESS_KEY_ID=YOUR_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=YOUR_SECRET_ACCESS_KEY

# S3 Configuration
S3_BUCKET_MODELS=forex-ai-models
S3_BUCKET_DATA=trading-data-bucket

# DynamoDB Tables (Auto-generated during deployment with environment suffix)
DYNAMODB_CRYPTO_TABLE=CryptoPrices-production
DYNAMODB_FOREX_TABLE=ForexPrices-production  
DYNAMODB_PORTFOLIO_TABLE=PortfolioState-production
DYNAMODB_NEWS_TABLE=NewsData-production

# API Configuration (Optional - Free APIs used by default)
# The system uses free APIs by default. Add keys below only for higher rate limits.
# CoinGecko Free API: https://www.coingecko.com/en/api (no key needed for basic usage)
# Alpha Vantage Free: https://www.alphavantage.co/support/#api-key (500 requests/day)
# NewsAPI Free: https://newsapi.org/ (1000 requests/day)
COINGECKO_API_KEY=demo_key_not_required_for_free_tier
ALPHA_VANTAGE_API_KEY=demo_key_get_from_alphavantage_co
NEWS_API_KEY=demo_key_get_from_newsapi_org

# No API Gateway required - Lambda functions triggered by CloudWatch Events

# Trading Settings
INITIAL_BALANCE=50.0
TRADING_CURRENCY=USD
AI_TRAINING_INTERVAL_HOURS=6

# Lambda Function Names
LAMBDA_CRYPTO_FETCHER=CryptoDataFetcher
LAMBDA_FOREX_FETCHER=ForexDataFetcher
LAMBDA_AI_TRAINER=AIModelTrainer
LAMBDA_PAPER_TRADER=PaperTrader

# Step Function ARN (Auto-generated during deployment)
# This will be automatically created when you deploy the SAM stack
# The ARN format: arn:aws:states:us-east-1:ACCOUNT_ID:stateMachine:TradingWorkflow
STEP_FUNCTION_ARN=will_be_generated_during_deployment